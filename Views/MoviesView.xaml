<<<<<<< HEAD
﻿<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             x:Class="XtreamIPTV.Views.MoviesView"
             d:DesignHeight="720" d:DesignWidth="1280">
=======
﻿<UserControl x:Class="XtreamIPTV.Views.MoviesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="420"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
<<<<<<< HEAD
            <LinearGradientBrush StartPoint="0.31,0.3" EndPoint="1,1">
=======
            <LinearGradientBrush StartPoint="0.3,0.5" EndPoint="1,1">
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                <GradientStop Color="#1D252B" Offset="0.0" />
                <GradientStop Color="White" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>
        <!-- CATEGORY LIST -->
<<<<<<< HEAD
        <ListBox Grid.RowSpan="2" Background="#1D252B" Foreground="White" FontSize="18" BorderThickness="0"
=======
        <ListBox Grid.RowSpan="2" Background="#2B373E" Foreground="White" FontSize="18" BorderThickness="0"
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                 ItemsSource="{Binding Categories}" 
                 SelectedItem="{Binding SelectedCategory}"
                 SelectionChanged="ListBox_SelectionChanged_1"
                 DisplayMemberPath="Name" 
<<<<<<< HEAD
                 Height="{Binding ActualHeight, ElementName=LayoutRoot, TargetNullValue=100}"/>
        <!-- MOVIE DETAILS -->

        <Canvas Grid.Row="0" Grid.Column="1" x:Name="MovieInfoCanvas" Height="390" >
            <Image Source="{Binding SelectedMovie.Backdrop, TargetNullValue={x:Null}}" Height="420" Canvas.Right="20" Canvas.Top="-15"/>
            <StackPanel Width="800" HorizontalAlignment="Left" VerticalAlignment="Top" Canvas.Top="8" Canvas.Left="8">
                <TextBlock Margin="5,0,0,0">
                    <Hyperlink NavigateUri="{Binding SelectedMovie.NavigaionUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                        <Run Text="{Binding SelectedMovie.Title}" FontSize="25" FontWeight="Bold" Foreground="White"/>
                    </Hyperlink>
                </TextBlock>
                <TextBlock Text="{Binding SelectedMovie.ReleaseInfo, TargetNullValue=''}" FontSize="15" TextWrapping="Wrap" Foreground="White" Margin="0,5,0,0" />
            </StackPanel>
            <ScrollViewer Height="250" Width="820" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Canvas.Top="70" Canvas.Left="8" HorizontalAlignment="Center" VerticalAlignment="Top">
                <StackPanel Width="800" HorizontalAlignment="Left" VerticalAlignment="Top" >
                    <!--<TextBlock Text="{Binding SelectedMovie.Title, TargetNullValue=''}" FontSize="25" FontWeight="Bold" TextWrapping="Wrap" Foreground="White" />-->
                    <TextBlock Text="{Binding SelectedMovie.Plot, TargetNullValue=''}" FontSize="20" TextWrapping="Wrap" Foreground="White" Margin="0,5,0,0" />
                    <WrapPanel x:Name="DirectorsWrapPanel" Width="800" Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Directors:" FontSize="12" TextWrapping="Wrap" Foreground="White"/>
                        <ItemsControl x:Name="DirectorsItemsControl" ItemsSource="{Binding SelectedMovie.Directors}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="5,0,0,0">
                                    <Hyperlink x:Name="DirectorHyperlink" NavigateUri="{Binding Url}" RequestNavigate="DirectorHyperlink_RequestNavigate">
                                        <Run Text="{Binding Text}" FontSize="12" Foreground="White"/>
                                    </Hyperlink>
                                    </TextBlock>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <WrapPanel.Style>
                            <Style TargetType="WrapPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <!-- If Count is 0, hide the parent -->
                                    <DataTrigger Binding="{Binding ElementName=DirectorsItemsControl, Path=Items.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </WrapPanel.Style>
                    </WrapPanel>
                    <WrapPanel x:Name="ActorsWrapPanel" Width="800" Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Actors:" FontSize="12" TextWrapping="Wrap" Foreground="White"/>
                        <ItemsControl x:Name="ActorsItemsControl" ItemsSource="{Binding SelectedMovie.Actors}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Margin="5,0,0,0" >
                                    <Hyperlink x:Name="ActorHyperlink" NavigateUri="{Binding Url}" RequestNavigate="ActorHyperlink_RequestNavigate">
                                        <Run Text="{Binding Text}" FontSize="12" Foreground="White"/>
                                    </Hyperlink>
                                    </TextBlock>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <WrapPanel.Style>
                            <Style TargetType="WrapPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <!-- If Count is 0, hide the parent -->
                                    <DataTrigger Binding="{Binding ElementName=ActorsItemsControl, Path=Items.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </WrapPanel.Style>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>
            <Button x:Name="PlayButton" Content="Play" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="175" Click="Button_Click"/>
            <Button x:Name="PlayButton2" Content="External" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="5" Click="PlayButton2_Click"/>
            <Button x:Name="FavoriteButton" Content="Favorite" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="345" Click="FavoriteButton_Click"/>
            <Button x:Name="SimiliarButton" Content="Similiar" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="515" Click="SimiliarButton_Click"/>
            <Button x:Name="RemoveHistButton" Content="Remove" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="685" Click="RemoveHistButton_Click"/>
            <Button x:Name="BackButton" Content="Back" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="685" Click="BackButton_Click" Visibility="Hidden"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Canvas.Top="-14">

                <ToggleButton x:Name="SortTogglePopupButton" Content="Sort" Margin="5,0,0,0" FontSize="15" />

                <Popup IsOpen="{Binding IsChecked, ElementName=SortTogglePopupButton}"
                               PlacementTarget="{Binding ElementName=SortTogglePopupButton}"
                               Placement="Bottom"
                               AllowsTransparency="True" 
                               StaysOpen="False">
                    <Border BorderThickness="1" Background="LightBlue" Padding="0">
                        <StackPanel x:Name="SortPanel">
                            <Button Content="Default" Click="DefaultSort_Click" FontSize="20" FontWeight="Bold"/>
                            <Button Content="Date" Click="DateSort_Click" FontSize="20"/>
                            <Button Content="Name" Click="NameSort_Click" FontSize="20"/>
                            <Button Content="Popularity" Click="PopularitySort_Click" FontSize="20"/>
                        </StackPanel>
                    </Border>
                </Popup>

                <ToggleButton x:Name="DecadeTogglePopupButton" Content="Decade" Margin="5,0,0,0" FontSize="15" />

                <Popup IsOpen="{Binding IsChecked, ElementName=DecadeTogglePopupButton}"
                               PlacementTarget="{Binding ElementName=DecadeTogglePopupButton}"
                               Placement="Bottom"
                               AllowsTransparency="True" 
                               StaysOpen="False">
                    <Border BorderThickness="1" Background="LightBlue" Padding="0">
                        <StackPanel x:Name="DecadePanel"/>
                    </Border>
                </Popup>

                <ToggleButton x:Name="LanguageTogglePopupButton" Content="Language" Margin="5,0,0,0" FontSize="15" />

                <Popup IsOpen="{Binding IsChecked, ElementName=LanguageTogglePopupButton}"
                               PlacementTarget="{Binding ElementName=LanguageTogglePopupButton}"
                               Placement="Bottom"
                               AllowsTransparency="True" 
                               StaysOpen="False">
                    <Border BorderThickness="1" Background="LightBlue" Padding="0">
                        <StackPanel x:Name="LanguagePanel"/>
                    </Border>
                </Popup>
                <ToggleButton x:Name="AgeTogglePopupButton" Content="Age Rating" Margin="5,0,0,0" FontSize="15" />

                <Popup IsOpen="{Binding IsChecked, ElementName=AgeTogglePopupButton}"
                               PlacementTarget="{Binding ElementName=AgeTogglePopupButton}"
                               Placement="Bottom"
                               AllowsTransparency="True" 
                               StaysOpen="False">
                    <Border BorderThickness="1" Background="LightBlue" Padding="0">
                        <StackPanel x:Name="AgePanel"/>
                    </Border>
                </Popup>
            </StackPanel>
        </Canvas>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right" >
            <TextBlock Text="{Binding FilteredMovies.Count, StringFormat=\{0\}}" FontSize="20" Margin="10,0,0,0" Foreground="White" Background="#7F000000"/>
            <TextBlock Text="{Binding FilteredMoviesCount, StringFormat=' / \{0\}'}" FontSize="20" Margin="0,0,30,0" Foreground="White" Background="#7F000000"/>
        </StackPanel>
        <!-- MOVIE LIST -->
        <ListBox x:Name="MoviesListBox"  Grid.Row="1" Grid.Column="1" 
                 ItemsSource="{Binding FilteredMovies}" BorderThickness="0" Background="Transparent"
=======
                 Height="{Binding ElementName=LayoutRoot, Path=ActualHeight}"/>
        <!-- MOVIE DETAILS -->
        <Canvas Grid.Row="0" Grid.Column="1" x:Name="MovieInfoCanvas" Height="390" >
            <Image Source="{Binding SelectedMovie.Backdrop, TargetNullValue={x:Null}}" Height="420" Canvas.Right="20" Canvas.Top="-15"/>
            <StackPanel Width="800" HorizontalAlignment="Left" VerticalAlignment="Top" >
                <TextBlock Text="{Binding SelectedMovie.Title}" FontSize="25" FontWeight="Bold" TextWrapping="Wrap" Foreground="White" />
                <TextBlock Text="{Binding SelectedMovie.ReleaseInfo}" FontSize="15" TextWrapping="Wrap" Foreground="White" Margin="0,5,0,0" />
                <TextBlock Text="{Binding SelectedMovie.Plot}" FontSize="20" TextWrapping="Wrap" Foreground="White" Margin="0,5,0,0" />
                <TextBlock Text="{Binding SelectedMovie.DirectorInfo}" FontSize="10" TextWrapping="Wrap" Foreground="White" Margin="0,10,0,0"/>
                <TextBlock Text="{Binding SelectedMovie.CastInfo}" FontSize="10" TextWrapping="Wrap" Foreground="White" Margin="0,10,0,0"/>
            </StackPanel>
            <Button x:Name="PlayButton" Content="Play" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="5" Click="Button_Click"/>
            <Button x:Name="PlayButton2" Content="External" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="175" Click="PlayButton2_Click"/>
            <Button x:Name="FavoriteButton" Content="Favorite" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="345" Click="FavoriteButton_Click"/>
            <!--<Button x:Name="DownloadButton" Content="Download" Width="150" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="5" 
                Background="LightBlue" FontWeight="Bold" FontSize="25" Canvas.Bottom="5" Canvas.Left="515"/>-->

            <ToggleButton Name="TogglePopupButton" Content="..." Height="20" Width="30" Canvas.Top="-14" Canvas.Left="0"/>

            <Popup IsOpen="{Binding IsChecked, ElementName=TogglePopupButton}"
                           PlacementTarget="{Binding ElementName=TogglePopupButton}"
                           Placement="Bottom"
                           AllowsTransparency="True" 
                           StaysOpen="False">
                <Border BorderThickness="1" Background="LightBlue" Padding="0">
                    <StackPanel>
                        <Button Content="Sort By Default" Click="DefaultSort_Click"/>
                        <Button Content="Sort By Popularity" Click="PopularitySort_Click"/>
                        <Button Content="Sort By Date" Click="DateSort_Click"/>
                    </StackPanel>
                </Border>
            </Popup>
        </Canvas>
        <!-- MOVIE LIST -->
        <ListBox x:Name="MoviesListBox"  Grid.Row="1" Grid.Column="1" 
                 ItemsSource="{Binding FilteredMovies}" Background="Transparent" BorderThickness="0"
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                 SelectedItem="{Binding SelectedMovie}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="ListBox_SelectionChanged" ScrollViewer.ScrollChanged="ListBox_ScrollChanged" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
<<<<<<< HEAD
                    <Border BorderBrush="Black"  BorderThickness="4" Margin="0" Padding="0" CornerRadius="10" Background="Transparent">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Top" Background="Transparent" >
                            <Canvas Height="230" Width="154">
                                <Image Width="154" Source="{Binding Poster, TargetNullValue='https://motivatevalmorgan.com/wp-content/uploads/2016/06/default-movie-1-1-204x300.jpg'}" VerticalAlignment="Top" />
                                <StackPanel Orientation="Horizontal" Canvas.Top="198">
                                    <Path Data="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
                                                              Width="20"
                                                              Height="20"
                                                              Fill="Gold"/>
                                    <TextBlock Text="{Binding Rating, StringFormat='{}{0:0.0}'}"
                                                               FontSize="20" Margin="2,0,0,0"
                                                               VerticalAlignment="Center"/>
                                </StackPanel>
                                <!--<TextBlock Canvas.Left="0" Canvas.Top="0" Text="{Binding Path=Similiar}" Foreground="Yellow" FontSize="25" Background="#5F000000"/>-->
                            </Canvas>
                            <TextBlock x:Name="titleText" TextWrapping="Wrap" Width="150" Height="50" Text="{Binding Title, TargetNullValue=''}" FontSize="15"/>
=======
                    <Border BorderBrush="Black"  BorderThickness="5" Margin="0" Padding="0" CornerRadius="2" Background="Transparent">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Top" Background="Transparent" >
                            <Canvas Height="228" Width="150">
                                <Image Width="150" Source="{Binding Path=Poster, TargetNullValue={x:Null}}" VerticalAlignment="Top" />
                                <TextBlock Canvas.Left="0" Canvas.Top="198" Text="{Binding Path=Rating}" FontSize="20" Background="#7F000000"/>
                                <!--<TextBlock Canvas.Left="0" Canvas.Top="0" Text="{Binding Path=Similiar}" Foreground="Yellow" FontSize="25" Background="#5F000000"/>-->
                            </Canvas>
                            <TextBlock x:Name="titleText" TextWrapping="Wrap" Width="150" Height="50" Text="{Binding Path=Title}" FontSize="15"/>
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
<<<<<<< HEAD
                <Style TargetType="{x:Type ListBoxItem}">
=======
                <Style TargetType="ListBoxItem">
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True" >
                            <Setter Property="BorderBrush" Value="Black" />
                            <Setter Property="Background" Value="Pink" />
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="False" >
                            <Setter Property="BorderBrush" Value="White" />
                            <Setter Property="Background" Value="#2B373E" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
<<<<<<< HEAD
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="LightSteelBlue" />
                        </Trigger>
=======
>>>>>>> 363c62477059520f3013ca59559e4972dc8805c4
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>